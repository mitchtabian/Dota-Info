dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "DotaInfo"

include ":app"

include ":core"

include ':hero'

include ':heroDetailUI'

include ':herosUI'

include ':navigation'

include ':player'

include ':topPlayersUI'

include ':topPlayersDetailUI'

include ':heroInteractors'
